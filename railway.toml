[build]
builder = "nixpacks"
buildCommand = "composer install"

[deploy]
startCommand = "php", "artisan", "serve", "--host=0.0.0.0", "--port=8081"
healthcheckPath = "/"
healthcheckTimeout = 100

[phases.setup]
nixPkgs = ['php81', 'php81Extensions.pdo_mysql', 'composer'] 